<html>
    <head>
        <title>
            <%= title %>
        </title>
    </head>
    <body>
        <h1>Playground</h1>
        <ul>
            <% for (let i=1; i<=10; i++) { %>
                <li>
                    <%= i %>
                </li>
            <% } %>
        </ul>
        <% if (false) { %>
            <p>It's true!</p>
        <% } else { %>
            <p>It's false (:</p>
        <% } %>
    </body>
</html>